export interface Configuration {
    enable: boolean;
    /** Skip the Svelte detection and assume this is a Svelte project */
    assumeIsSvelteProject: boolean;
}
export declare class ConfigManager {
    private emitter;
    private config;
    onConfigurationChanged(listener: (config: Configuration) => void): void;
    isConfigChanged(config: Configuration): boolean;
    updateConfigFromPluginConfig(config: Configuration): void;
    getConfig(): Configuration;
}
